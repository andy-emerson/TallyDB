[package]
name = "query-lite"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
arrow-lite = { path = "../arrow-lite" }
storage-lite = { version = "0.0.1", path = "../storage-lite" }
sqlparser = "0.62.0"

[dev-dependencies]
# Deliberately empty of an oracle dependency. The DuckDB differential
# runs from Python against the `engine` cdylib (crates/engine/tests/
# m2_differential_oracle.py), so it exercises the whole vertical slice
# and DuckDB never enters this workspace's dependency graph at all —
# see DESIGN.md on why DataFusion's executor is not vendored or linked.
