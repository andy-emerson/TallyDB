# Release builds — the M3 GA deliverable (#39): one static-feeling
# binary per platform, attached to the GitHub Release for a version
# tag. The binary is the console; the library ships as the crate, which
# publish.yml uploads on a manual run — this workflow never uploads
# to crates.io.
name: Release
on:
  push:
    tags: ["v*"]
permissions:
  contents: write
jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact: tallydb-linux-x86_64
          - os: macos-latest
            artifact: tallydb-macos-arm64
          - os: windows-latest
            artifact: tallydb-windows-x86_64.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      # --locked so the shipped binary is built from the committed
      # lockfile — the dependency set CI tested (#110) — rather than
      # from whatever cargo would resolve on release day.
      - name: Build the console binary
        run: cargo build --release --locked
      - name: Name the artifact (unix)
        if: runner.os != 'Windows'
        run: cp target/release/tallydb ${{ matrix.artifact }}
      - name: Name the artifact (windows)
        if: runner.os == 'Windows'
        run: copy target\release\tallydb.exe ${{ matrix.artifact }}
      - name: Attach to the release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
